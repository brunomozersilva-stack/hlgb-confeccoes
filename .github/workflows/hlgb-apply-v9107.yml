name: HLGB legacy v91.07 installer

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  legacy:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Instalador legado v91.07 desativado para pushes. A versão ativa é superior.'
